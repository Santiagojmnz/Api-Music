const { albumRegister } = require('./register');

const {findAlbum, findAlbumId, albumPaginate} = require('./findAlbum');

module.exports = {
    albumRegister,
   



    findAlbum,
    findAlbumId,
    albumPaginate
}